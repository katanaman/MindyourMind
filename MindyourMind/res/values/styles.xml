<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Colours to use in the app -->
    <color name="primary">#1eabf2</color>
    <color name="primary_dark">#178acf</color>
    <color name="accent">#ffb936</color>
    <color name="window_background">#c1dae6</color>
    <color name="border_color">#000080</color>
    
    <!-- This theme will be overridden in newer SDKs -->
    <style name="AppTheme" parent="AppTheme.Base"></style>

    <!-- Application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar">
        
		<item name="colorPrimary">@color/primary</item>
		<item name="colorPrimaryDark">@color/primary_dark</item>
		<item name="colorAccent">@color/accent</item>
		
		<item name="android:windowBackground">@color/window_background</item>
		
    </style>
    
    <!-- theme to be overridden in newer SDKs -->
    <style name="AppTheme.TransparentActionBar" parent="AppTheme.TransparentActionBar.Base"></style>
    
    <style name="AppTheme.TransparentActionBar.Base" parent="AppTheme">
        
        <!-- <item name="android:windowTranslucentStatus">true</item> -->
        <item name="android:windowActionBarOverlay">true</item>
        <!-- <item name="android:windowTranslucentNavigation">true</item> -->
        <item name="android:actionBarStyle">@style/TransparentActionBar</item>
    </style>
    
    <!--  transparentActionBar style -->
    <style name="TransparentActionBar" parent = "Theme.AppCompat">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
